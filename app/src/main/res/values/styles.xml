<resources>

    <!-- Base application theme. -->
    <style name="BaseAppTheme" parent="Theme.AppCompat.NoActionBar" />
    <!-- Base application theme. -->
    <style name="AppThemeNotFullscreen" parent="@style/BaseAppTheme">
        <!--<item name="android:fontFamily">@font/dinpro_medium</item>-->
        <!--<item name="android:windowAnimationStyle">@style/ActivityAnimation</item>-->
    </style>

    <style name="AppThemeNotFullscreen.Launcher" parent="AppThemeNotFullscreen">
<!--        <item name="android:windowBackground">@color/standardWhite</item>-->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>
